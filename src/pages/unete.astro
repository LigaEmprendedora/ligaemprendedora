---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---

<BaseLayout
  title="Únete | Liga Emprendedora"
  description="Únete gratis a Liga Emprendedora: recibe contenido útil, accede a la Pretemporada 2026 y entra al vestuario donde entrenan emprendedores de LATAM."
>
  <div class="flex min-h-screen flex-col bg-[#f4f6fa]">
    <Header />

    <main class="flex-1">

      <!-- ======================= HERO ======================= -->
      <section class="w-full px-4 py-16 md:px-6 md:py-24 text-center">
        <div class="mx-auto max-w-3xl">
          <p class="text-[11px] font-semibold uppercase tracking-[0.25em] text-[#162741]/80">
            Pretemporada 2026 · Cupos Limitados
          </p>

          <h1 class="mt-3 text-3xl md:text-5xl font-extrabold text-[#162741] leading-tight">
            Únete gratis a la Liga.<br />
            Empieza a jugar tu partido.
          </h1>

          <p class="mt-4 text-sm md:text-lg text-[#444] max-w-xl mx-auto">
            Entra al vestuario, recibe contenido útil, retos semanales, recursos y acceso anticipado
            al grupo privado de emprendedores en LATAM.
          </p>
        </div>
      </section>

      <!-- ======================= FORMULARIO ======================= -->
      <section class="w-full px-4 pb-20 md:px-6">
        <div class="mx-auto max-w-xl rounded-4xl bg-white p-8 shadow-lg border border-[#ececec]">

          <h2 class="text-xl md:text-2xl font-bold text-[#162741] text-center mb-6">
            🎟️ Inscríbete gratis a la Pretemporada 2026
          </h2>

          <form
            name="unete"
            method="POST"
            data-netlify="true"
            netlify-honeypot="bot-field"
            class="space-y-4"
          >
            <input type="hidden" name="form-name" value="unete" />

            <p class="hidden">
              <label>No llenar: <input name="bot-field" /></label>
            </p>

            <!-- Nombre -->
            <div>
              <label class="block text-xs font-semibold text-[#4b5563]">
                Nombre completo*
              </label>
              <input
                type="text"
                name="nombre"
                required
                class="mt-1 w-full rounded-2xl border border-[#e5e7eb] px-4 py-2.5 text-sm outline-none focus:border-[#162741]"
              />
            </div>

            <!-- Email -->
            <div>
              <label class="block text-xs font-semibold text-[#4b5563]">
                Email*
              </label>
              <input
                type="email"
                name="email"
                required
                class="mt-1 w-full rounded-2xl border border-[#e5e7eb] px-4 py-2.5 text-sm outline-none focus:border-[#162741]"
              />
            </div>

            <!-- País -->
            <div>
              <label class="block text-xs font-semibold text-[#4b5563]">
                País
              </label>
              <input
                type="text"
                name="pais"
                class="mt-1 w-full rounded-2xl border border-[#e5e7eb] px-4 py-2.5 text-sm outline-none focus:border-[#162741]"
              />
            </div>

            <!-- ¿Qué estás emprendiendo? -->
            <div>
              <label class="block text-xs font-semibold text-[#4b5563]">
                ¿Qué estás emprendiendo?
              </label>
              <textarea
                name="proyecto"
                rows="3"
                class="mt-1 w-full rounded-2xl border border-[#e5e7eb] px-4 py-2.5 text-sm outline-none focus:border-[#162741]"
                placeholder="Idea o negocio actual"
              ></textarea>
            </div>

            <!-- CTA -->
            <button
              type="submit"
              class="mt-4 w-full rounded-full bg-[#162741] px-6 py-3 text-sm md:text-base
                     font-semibold text-white shadow-md transition hover:bg-[#c23a26]"
            >
              Unirme a la Liga
            </button>

            <p class="mt-3 text-[11px] text-center text-[#6b7280]">
              Sin spam. Sin ruido. Solo contenido útil para emprendedores.
            </p>
          </form>
        </div>
      </section>

      <!-- ======================= EXTRA COPY ======================= -->
      <section class="w-full px-4 pb-24 md:px-6 text-center">
        <h3 class="text-xl font-semibold text-[#162741]">
          Aquí no hay banca. Todos juegan.
        </h3>
        <p class="mt-2 text-sm text-[#555] max-w-lg mx-auto">
          Al unirte, recibes acceso al vestuario: ideas, herramientas, comunidad y
          contenido accionable para avanzar cada semana.
        </p>
      </section>

    </main>

    <Footer />
  </div>
</BaseLayout>
